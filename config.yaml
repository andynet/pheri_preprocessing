main_dir: '/home/andrej/Projects/pheri_preprocessing'
data_dir: '/home/andrej/Projects/pheri_preprocessing_data'

databases: ['genbank', 'refseq', 'phagesdb']
cluster_method: ['needle_mcl', 'mcl', 'spectral']
type: ['fs', 'raw']

threads: 16
variance_threshold: 0.01

# if list is empty, hosts are determined automatically based on train_num
# else variable hosts is used
hosts: []
train_num: 50
train_perc: 0.8